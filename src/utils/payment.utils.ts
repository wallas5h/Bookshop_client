export enum PayMethodName {
  card = "Payment card",
  gpay = "Google Pay",
  blik = "BLIK",
  transfer = "Transfer",
}

export enum PayMethodType {
  card = "card",
  gpay = "",
  blik = "blik",
  transfer = "p24",
}

export enum DeliveryName {
  paczkomat = "paczkomat",
  courier = "courier",
}

export enum DeliveryPrice {
  paczkomat = 3.99,
  courier = 4.99,
}
